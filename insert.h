#ifndef INSERT_H
#define INSERT_H
#include "global.h"
void insert(goods *headaq, const string& name, const string& brand, int num, float price);
class insert
{
public:
    insert();
};

#endif // INSERT_H
